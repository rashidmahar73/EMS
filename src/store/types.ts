type eventType = {
  id: number
  eventName: string
  thumbnail: string
  date: string
  location: string
}

type userTypes = { username: string; email: string; phoneNumber: number }

export type { eventType, userTypes }
